import { ValidateResourcesIdInterceptor } from './validate-resources-id.interceptor';

describe('ValidateResourcesIdInterceptor', () => {
  it('should be defined', () => {
    expect(new ValidateResourcesIdInterceptor()).toBeDefined();
  });
});
